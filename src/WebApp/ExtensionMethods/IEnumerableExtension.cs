using System;
using System.Collections.Generic;
/*
namespace WebApplicationAPI.ExtensionMethods {
    public static class EnumerableVoid {
        public static void ForEach<T>(this IEnumerable<T> target, Action<T> action) {
            foreach (T item in target) action(item);
        }
    }

    public static class EnumerableTResult {
        public static IEnumerable<TOut> ForEach<TIn, TOut>(this IEnumerable<TIn> target, Func<TIn, TOut> function) {
            foreach (TIn item in target) yield return function(item);
        }
    }
}
*/
